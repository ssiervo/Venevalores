PORT=4000
# Optional manual override if the BCV source is unavailable
# BCV_EXCHANGE_RATE_MANUAL=40.25
# Optional fallback constant if all providers fail
# BCV_EXCHANGE_RATE_FALLBACK=36.5
# SMTP configuration to send broker emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-user
# SMTP_PASSWORD=your-password
