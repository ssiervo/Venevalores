PORT=4000
NODE_ENV=development
JWT_SECRET=venevalores-dev

# Optional manual override if the BCV source is unavailable
# BCV_EXCHANGE_RATE_MANUAL=40.25
# Optional fallback constant if all providers fail
# BCV_EXCHANGE_RATE_FALLBACK=36.5
# SMTP configuration to send broker emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-user
# SMTP_PASSWORD=your-password

# choose: bvc_scrape | sheet | dummy
MARKET_DATA_PROVIDER=bvc_scrape

# bvc_scrape
BVC_BASE_URL=https://www.bolsadecaracas.com
CACHE_TTL_SECONDS=180
REQUEST_USER_AGENT=VenevaloresBot/1.0

# sheet (optional)
SHEET_CSV_URL=
